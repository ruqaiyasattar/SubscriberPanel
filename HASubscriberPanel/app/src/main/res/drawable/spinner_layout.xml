<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item>
        <!-- create gradient you want to use with the angle you want to use -->
        <shape android:shape="rectangle" >
            <gradient
                android:angle="45"
                android:centerColor="@color/pri_color_one"
                android:endColor="@color/pri_color_two"
                android:startColor="@color/sec_color_one" />

        </shape>
    </item>
    <!-- create the stroke for top, left, bottom and right with the dp you want -->
    <item
        android:bottom="1dp"
        android:left="1dp"
        android:right="1dp"
        android:top="1dp">
        <shape android:shape="rectangle" >
            <!-- fill the inside in the color you want (could also be a gradient again if you want to, just change solid to gradient and enter angle, start, maybe center, and end color) -->
            <solid android:color="#F7F7F7" />
        </shape>
    </item>

    <item android:right="5dp">

        <bitmap android:gravity="center_vertical|right" android:src="@drawable/dropdown" />

    </item>

</layer-list>